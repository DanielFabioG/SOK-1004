## Introduksjon

Dette dokumentet er laget i Quarto. Her kan man sy sammen kode med annet innhold i et ferdig dokument. LC&r mer om Quarto pC% <https://quarto.org>.

## KjC8re kode

Koden kan kjC8res interaktivt.

```{r}
rm(list=ls())
library(tidyverse)
```

Vi fC%r en utskrift av koden som ville ha blitt vist i konsollen.

FC8lgende kode henter dataene vi brukte i Forelesning 3.

```{r}
#| output: false

link <- 
  "https://raw.githubusercontent.com/owid/co2-data/master/owid-co2-data.csv"
co2data <- read_csv(link)
```

Her har jeg valgt C% ikke vise utskrift av konsollen i det ferdige dokumentet. SC% lager jeg en figur:

```{r}
co2data %>%
  group_by(year) %>% 
  summarise(co2=sum(co2, na.rm = TRUE)) %>% 
  filter(year >= 1800) %>%
  ggplot(aes(x=year, y=log(co2))) %>% 
  + geom_line(color = "red", lwd=1) %>%
  + theme_minimal() %>%
  + xlab("Cr") %>%
  + ylab("Log av millioner tonn CO2") %>%
  +  ggtitle("Globale CO2 utslipp per C%r, logaritmisk skala, 1800 - 2020") 
```
